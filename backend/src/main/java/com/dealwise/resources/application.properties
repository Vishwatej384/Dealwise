# src/main/resources/application.properties

# Server port (keep 8080 if Tomcat embedded; change if conflict)
server.port=8080

# MySQL connection (update username/password to match your local MySQL)
dealwise.db.url=jdbc:mysql://localhost:3306/dealwise_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
dealwise.db.user=root
dealwise.db.password=your_mysql_password_here

# JWT secret (for dev only) — change before deploying publicly
dealwise.jwt.secret=ReplaceThisWithAStrongSecretKeyAtLeast32BytesLong!
